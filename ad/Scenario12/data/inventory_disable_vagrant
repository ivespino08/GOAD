[default]
; Note: ansible_host *MUST* be an IPv4 address or setting things like DNS
; servers will break.
; Use this inventory instead of the provider inventory when running
; directly against existing Linux hosts without Vagrant.
; ------------------------------------------------
; Scenario12 -- Docker host (trimmed to the one host that carries a vuln)
; ------------------------------------------------
docker-6     ansible_host={{ip_range}}.16  dict_key=docker-6

; ADDED (this session) -- see providers/vmware/inventory's note: this
; group was missing from both inventory files you uploaded, and every
; topology-*.yml in this scenario needs it to match anything at all.
[docker_nodes]
docker-6

[all:vars]
domain_name=Scenario12
ip_range=192.168.57
ansible_user=ubuntu
ansible_connection=ssh
ansible_python_interpreter=/usr/bin/python3
ansible_ssh_private_key_file=~/.ssh/id_rsa
ansible_ssh_common_args='-o StrictHostKeyChecking=no'
