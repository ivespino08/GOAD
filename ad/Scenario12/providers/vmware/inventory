[default]
docker-6    ansible_host={{ip_range}}.16  dict_key=docker-6    ansible_ssh_private_key_file=~/.vagrant.d/insecure_private_key

[domain]
docker-6

; ADDED (this session): this group didn't exist in either inventory file
; you uploaded -- only [default]/[domain] were present. Every
; topology-*.yml in this scenario targets "hosts: docker_nodes", so
; without this group none of them would have matched any host at all,
; trim or no trim. Looks like the real data/inventory (the file that
; normally carries this group, per the same gap found in Scenario3) isn't
; among what got uploaded here either.
[docker_nodes]
docker-6
