# PLACEHOLDER -- not the real generator output, and almost certainly wrong *format*.
# Real file: /tmp/vulns/flag_generators_runs/flow-newscenario22/02_binary_embed_text_docker-2/artifacts/secrets.txt
# The generator name ("Binary Embed Text") strongly implies the real artifact is a
# binary file with the flag string embedded in it (e.g. strings-extractable from an
# ELF/blob), not plain ASCII like this stand-in. Please swap in the real file --
# only the flag value below is confirmed correct (from Challenge200_solution.json).
FLAG{3a62a74dbf8aa160301c583e}
