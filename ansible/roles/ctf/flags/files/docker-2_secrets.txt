# PLACEHOLDER -- not the real generator output, and almost certainly the wrong format.
# Real file: /tmp/vulns/flag_generators_runs/flow-newscenario22/02_binary_embed_text_docker-2/artifacts/secrets.txt
# Generator name ("Binary Embed Text") implies a binary artifact with the flag
# embedded in it, not plain ASCII like this stand-in. Only the flag value below is
# confirmed correct (from Challenge5_solution.json).
FLAG{22253af8d765055d9120862a}
